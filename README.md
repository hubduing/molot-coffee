# МОЛОТ·КОФЕ — спешалти кофейня (React + Vite)`n`nЛендинг + личный кабинет «МОЛОТ». React (Vite, JS): меню с табами, бронь столика, регистрация/вход через Supabase Auth с fallback в localStorage.`n`nРепозиторий: https://github.com/hubduing/molot-coffee.git · ветка: feat/react-migration`n`n## Быстрый старт`n`n```powershell`ncd "D:\1WORK\AI\063"`nnpm install`nnpm run dev      # http://localhost:5173`nnpm run build    # прод-сборка в dist/`nnpm run preview  # просмотр сборки`n```n`n## Структура`n`n- index.html, vite.config.js, src/main.jsx, src/App.jsx, src/index.css (стили 1-в-1 из legacy)`n- src/data/menu.js, src/lib/supabase.js (.env), src/lib/auth.js (Supabase + local fallback)`n- src/context/AuthContext.jsx, src/components/ (Navbar, Hero, MenuSection, Story, BookingForm, Footer, AuthModal, Toast)`n- legacy/ — старая статика до приёмки; supabase-schema.sql — без изменений`n`n## Supabase`n`n1. Выполнить supabase-schema.sql в SQL Editor. 2. Скопировать anon key в .env.local (см. .env.example: VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY). 3. Включить Email (и Google при желании) в Authentication > Providers. Без ключей — локальный режим mode: local.`n`n## Контакты (демо)`n`n- ул. Прожжарная, 12с2 · +7 (495) 120-14-14 · hello@molot.coffee
